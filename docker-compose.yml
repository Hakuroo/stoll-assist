services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: stoll
      POSTGRES_PASSWORD: stoll
      POSTGRES_DB: stoll_assist
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres/init.sql:/docker-entrypoint-initdb.d/001-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stoll -d stoll_assist"]
      interval: 5s
      timeout: 5s
      retries: 10

  postgres_test:
    image: pgvector/pgvector:pg17
    profiles: ["test"]
    environment:
      POSTGRES_USER: stoll
      POSTGRES_PASSWORD: stoll
      POSTGRES_DB: stoll_assist
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stoll -d stoll_assist"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  api:
    build:
      context: ./services/api
      target: production
    environment:
      AUTH_COOKIE_SECURE: "false"
      AUTH_ALLOWED_ORIGINS: "http://localhost:3000,http://127.0.0.1:3000"
      WHATSAPP_SEND_ENABLED: "false"
      WHATSAPP_SEND_LEASE_SECONDS: "120"
    env_file:
      - path: .env
        required: false
    ports:
      - "8000:8000"
    volumes:
      - ./services/api:/app
      - ./config:/app/config:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      minio:
        condition: service_started
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  worker:
    build:
      context: ./services/api
      target: production
    env_file:
      - path: .env
        required: false
    volumes:
      - ./services/api:/app
      - ./config:/app/config:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    command: python -u -m app.worker

  dashboard:
    build:
      context: ./apps/dashboard
    environment:
      API_BASE_URL: http://api:8000
      DASHBOARD_TENANT_SLUG: grupo-stoll
      DASHBOARD_OPERATOR_NAME: Operador local
    ports:
      - "3000:3000"
    volumes:
      - ./apps/dashboard:/app
      - dashboard_node_modules:/app/node_modules
    depends_on:
      api:
        condition: service_started

  test:
    profiles: ["test"]
    build:
      context: ./services/api
      target: test
    environment:
      APP_ENV: test
      DATABASE_URL: postgresql+psycopg://stoll:stoll@postgres_test:5432/stoll_assist
      REDIS_URL: redis://redis:6379/0
      WEBHOOK_QUEUE_NAME: stoll:test:webhooks
      META_VERIFY_TOKEN: test-token
      META_APP_SECRET: replace-me
      META_ACCESS_TOKEN: not-used-in-tests
      META_PHONE_NUMBER_ID: test-phone-number-id
      META_API_VERSION: v99.0
      WHATSAPP_SEND_ENABLED: "false"
      WHATSAPP_REQUEST_TIMEOUT_SECONDS: "10"
      WHATSAPP_SEND_LEASE_SECONDS: "120"
      OPENAI_API_KEY: ${OPENAI_API_KEY:-not-used-in-tests}
      OPENAI_GENERATION_MODEL: ${OPENAI_GENERATION_MODEL:-gpt-5.4-mini}
      LLM_DRAFTING_ENABLED: "false"
      LLM_DRAFTING_PROVIDER: openai
      LLM_DRAFTING_LEASE_SECONDS: "60"
      RUN_LIVE_OPENAI_TEST: ${RUN_LIVE_OPENAI_TEST:-0}
      KNOWLEDGE_CONFIG_PATH: /app/config/stoll/knowledge
      AUTH_COOKIE_SECURE: "false"
      AUTH_ALLOWED_ORIGINS: "http://localhost:3000,http://127.0.0.1:3000"
      PGHOST: postgres_test
      PGUSER: stoll
      PGPASSWORD: stoll
      PGDATABASE: stoll_assist
    volumes:
      - ./config:/app/config:ro
      - ./infra/postgres/init.sql:/app/init.sql:ro
      - ./infra/postgres/migrations:/app/migrations:ro
    depends_on:
      postgres_test:
        condition: service_healthy
      redis:
        condition: service_started


volumes:
  postgres_data:
  redis_data:
  minio_data:
  dashboard_node_modules:
